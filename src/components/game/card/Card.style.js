import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: '80%',
    alignItems: 'center',
    borderRadius: 20,
    elevation: 14,
    alignContent: 'center',
  },
});
